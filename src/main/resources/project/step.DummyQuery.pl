$[/myProject/perl/perlObjects.pm]
package main;

$|=1;

# protocol required
my $poolName    = "$[poolName]";
my $current     = "$[current]";

## hard coded test value
my $agents      = "$[agents]";

$::pdb->setProp("/myJob/CloudManager/query",$agents);

exit 0;


